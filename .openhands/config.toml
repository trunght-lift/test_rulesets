# OpenHands config - dùng cho headless mode
# Các giá trị này bị override bởi env vars LLM_API_KEY / LLM_MODEL khi cần

[llm]
model = "anthropic/claude-sonnet-4-5"
# api_key sẽ được inject qua env LLM_API_KEY

[agent]
# Không cần confirmation trong headless
confirmation_mode = false
